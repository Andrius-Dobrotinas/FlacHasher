For information on what this is for refer to readme.txt.

The following commands are available:
  - hash (operation name is optional, it executes by default)
  - verify

Syntax:
  flachasher [command] [--param=value]

for example:
  flachasher hash --input=c:\muzak\slts.flac --algorithm=md5

Where value is "true"/"false", the value can be completely omitted to provide "true", eg:
  flachash --fail-fast

=====
1. Settings file: {SETTINGS_FILE_NAME}

=====
2. Decoder Configuration:

{DECODER_CONFIG}

====
3.1. Hashing

{HASHING_PARAMS}

====
3.2. Verification

{VERIFICATION_PARAMS}