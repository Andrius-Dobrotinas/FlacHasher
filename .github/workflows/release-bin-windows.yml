name: Release the Compiled Application (PLACEHOLDER)

on:
  workflow_dispatch:

jobs:
  windows-base:
    runs-on: windows-latest
    steps:
      - shell: pwsh
        run: Write-Host "Placeholder workflow"